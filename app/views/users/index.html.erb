<h1>Listing Users</h1>

<table>
  <tr>
    <th>Email</th>
    <th>Admin</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.is_admin %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to "New user", new_user_path %>